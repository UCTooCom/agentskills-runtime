[package]
  cjc-version = "1.0.5"
  name = "http2_push_integration"
  version = "1.0.0"
  output-type = "executable"
  compile-option = "-Woff unused"
  src-dir = "src"
  target-dir = "target"

[dependencies]
  http_lib = { path = "../.." }
