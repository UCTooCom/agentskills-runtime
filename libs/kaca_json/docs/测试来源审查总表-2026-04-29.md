# 测试来源审查总表（JSONTestSuite / test262 / json5）
日期：2026-04-29
审查范围：`kaca_json`

## 审查目标
1. 是否迁移了测试数据。
2. 是否实现了对应测试代码。
3. 是否通过了测试。

## 总览
| 来源 | 上游路径 | 本地上游克隆存在 | 数据迁移状态 | 测试代码实现状态 | 最新测试状态 |
|---|---|---|---|---|---|
| JSONTestSuite | `C:\Users\27482\Desktop\JSONTestSuite` | 是 | 已迁移（子集） | 已实现 | 通过 |
| test262 | `C:\Users\27482\Desktop\test262` | 是 | 已迁移（子集） | 已实现 | 通过 |
| json5 | `C:\Users\27482\Desktop\json5` | 是 | 未迁移原始数据文件（采用用例映射） | 已实现（官方子集测试） | 通过 |

## 1) JSONTestSuite 审查
### 1.1 数据迁移
- 迁移目标目录：`testdata/kaca_json/standard/json_test_suite/`
- 迁移方式：按类别子集导入（非全量镜像）

| 类别 | 上游数量 | 已迁移数量 |
|---|---:|---:|
| number valid (`y_number*`) | 19 | 15 |
| number invalid (`n_number*`) | 51 | 17 |
| string valid (`y_string*`) | 43 | 12 |
| string invalid (`n_string*`) | 29 | 12 |
| structure valid (`y_structure*`) | 10 | 10 |
| structure invalid (`n_structure*`) | 49 | 10 |
| array valid (`y_array*`) | 11 | 10 |
| array invalid (`n_array*`) | 26 | 10 |
| object valid (`y_object*`) | 12 | 8 |
| object invalid (`n_object*`) | 28 | 10 |
| implementation (`i_*`) | 35 | 35（textual 22 + deferred_byte_boundary 13） |

结论：`JSONTestSuite` 已完成“可复现的分层子集迁移”，不是全量迁移。

### 1.2 测试代码实现
- 标准解析器测试：`tests/kaca_json/src/standard_parser_test.cj`
- JSON/Fast 一致性测试：`tests/kaca_json/src/legacy_and_fast_conformance_test.cj`
- 已覆盖 number/string/structure/array/object + `i_*` 分层策略。

### 1.3 通过状态
- 2026-04-29 在 WSL 执行：`cjpm test --no-color`
- 结果：通过（TOTAL 43, PASSED 43, FAILED 0）

## 2) test262 审查
### 2.1 数据迁移
- 迁移目标目录：`testdata/kaca_json/standard/test262/parse/source/`
- 上游 `JSON.parse` 目录文件数：77
- 已迁移文件数：32
- 迁移方式：精选词法/空白/转义规则子集（非全量）

结论：`test262` 已迁移并落地为子集语料。

### 2.2 测试代码实现
- `tests/kaca_json/src/standard_parser_test.cj`
  - `test262WhitespaceAndEscapeSubsetShouldMatchStandardBehavior`
  - `test262MappedSourceSnapshotsShouldExist`
- `tests/kaca_json/src/legacy_and_fast_conformance_test.cj`
  - `jsonAndFastShouldMatchTest262LexicalSubset`

### 2.3 通过状态
- 2026-04-29 在 WSL 执行：`cjpm test --no-color`
- 结果：通过（TOTAL 43, PASSED 43, FAILED 0）

## 3) json5 审查
### 3.1 数据迁移
- 上游测试文件存在：
  - `C:\Users\27482\Desktop\json5\test\parse.js`
  - `C:\Users\27482\Desktop\json5\test\errors.js`
- 本项目 `testdata/kaca_json/standard/` 下未建立 json5 原始语料镜像目录。
- 当前策略：将官方测试语义映射为仓颉测试用例（非文件迁移）。

结论：json5 目前是“用例映射驱动”，不是“测试数据文件迁移驱动”。

### 3.2 测试代码实现
- `tests/kaca_json/src/json5_official_subset_test.cj`
  - `officialValidSubsetShouldParse`
  - `officialInvalidSubsetShouldFail`
  - `officialNaNBehaviorShouldHold`
  - `officialObjectKeyAdvancedCases`
  - `officialStringEscapeCases`
  - `officialErrorSubsetFromErrorsJs`
  - `officialAdditionalParseCases`
  - `officialRemainingParseCoverage`
  - `officialRemainingErrorCoverage`

### 3.3 通过状态
- 2026-04-29 在 WSL 执行：`cjpm test --no-color`
- 结果：通过（TOTAL 43, PASSED 43, FAILED 0）

## 审查结论
1. 三个来源都已被本项目引用并转化为测试资产。
2. `JSONTestSuite` 与 `test262` 为“子集数据迁移 + 测试代码实现”。
3. `json5` 为“官方用例语义映射 + 测试代码实现”，尚未做原始测试文件镜像迁移。
4. 当前全量测试在 WSL 下通过（43/43）。

## 验收口径确认（2026-04-29）
1. 口径：`JSONTestSuite` 与 `test262` 只需迁移“本项目需要的测试”即可判定成功，不要求全量迁移。
2. 结论：按该口径，当前状态已达成（所需子集已迁移、测试代码已实现、全量测试通过）。

## 下次审查建议（延续本表）
1. 若目标是可追溯性更强，建议为 json5 增加 `testdata` 级别的 case 清单映射文件（case -> `parse.js/errors.js` 行号）。
2. 若目标是提高覆盖率，优先扩充 `JSONTestSuite` 的 `n_*` 大样本与 `test262` 剩余 parse 用例。
3. 每次新增/删减样例后，更新本表中的“上游数量/迁移数量/通过状态”。

## 问题修复记录（2026-04-29）
1. 已修复重复维护问题：提取共享清单 `tests/kaca_json/src/test_vectors_manifest.cj`，`standard_parser_test.cj` 与 `legacy_and_fast_conformance_test.cj` 改为共用同一份样例定义。
2. 已修复历史文案残留：`legacy_and_fast_conformance_test.cj` 失败信息已统一为 `JSON.parseFast` / `JSON.parseFast(...).toJsonValue()` 语义。
3. 已补齐 JSON5 可追溯映射：新增 `kaca_json/docs/json5-official-source-mapping-2026-04-29.md`（case -> 官方 `parse.js/errors.js` 行号）。
4. 已固化 surrogate 策略断言：新增
   - `StandardParserTests.escapedSurrogatePolicyShouldBeStrictAndDeterministic`
   - `LegacyAndFastConformanceTests.jsonAndFastShouldEnforceEscapedSurrogatePolicy`
   明确“成对 surrogate 通过，lone surrogate 失败”。
