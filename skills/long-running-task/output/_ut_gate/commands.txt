# some_cangjie_gen → cangjie-coder → code-gen-verifier
# [agent-skill] 由宿主 LLM 调度技能 cangjie-coder 补齐「some_cangjie_gen」（无 CLI 入口，不出可执行命令）
python ../code-gen-verifier/scripts/verify.py --files "${some_cangjie_gen.generated_files}" --project-path "${project_path}" --verify-level compile
