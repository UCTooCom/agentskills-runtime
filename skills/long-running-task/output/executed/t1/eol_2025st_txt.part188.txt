Variable(variable) {

            var query = window.location.search.substring(1);

            var vars = query.split("&");

            for (var i = 0; i < vars.length; i++) {

                var pair = vars[i].split("=");

                if (pair[0] == variable) { return pair[1]; }

            }

            return (false);

        }

        var fromcoop = getQueryVariable("fromcoop");

        function handleZy() {

            if (fromcoop) {

                window.open('https://mnzy.eol.cn?channel=' + fromcoop);

            } else {

                window.open('https://mnzy.eol.cn');

            }

        }



        // 添加右侧悬浮和底部直播动画

        function createAni(img) {

            img.classList.add("active");



            // 添加动画属性

            img.style.right = '50px'

            img.style.width = '0'

            img.style.opacity = 0

            img.style.bottom = 'calc(20% + 225px)'



            // 动画结束后，销毁

            img.ontransitionend = function () {

  